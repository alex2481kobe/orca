GOAL: HARDEN ORCA AND MAKE IT OBVIOUS TO USE. Audit this repository against the six real failures logged below — every one of which happened to a live orchestrator today, on this machine, in the last two hours — and produce an implementation-ready scope that (a) makes each failure structurally impossible or loudly self-diagnosing, and (b) brings the setup, daemon lifecycle and agent-facing surface up to what a competent outside adopter would expect. Deliver the scope; implement nothing.

CONTEXT:
YOU ARE AUDITING ORCA ITSELF at /Users/alexrodriguez/Documents/Projects/web/orca. Read README.md, docs/agent-orchestrator-skill.md, docs/agent-executor-skill.md, src/server.js, src/mcp-server.js, src/mcp-orchestrator-bootstrap.js, src/executor/constants.js and src/registry-lane-create.js. NOTE: the Orca daemon is RUNNING FROM THIS CHECKOUT right now on port 3000 with live state (3 projects, 11 orchestrators, 82 lanes). You are READ-ONLY; do not restart it, do not write, do not curl any mutating endpoint.

THE SIX FAILURES, ALL OBSERVED TODAY. Verify each against the source before scoping it; if the tree contradicts my account, THE TREE WINS and you must say so with file and line.

1. ORCA'S OWN INSTALL COMMAND REGISTERS THE MCP SERVER AT THE WRONG SCOPE. `mcp-orchestrator-bootstrap.js` emits `globalInstall.claudeCli` as `claude mcp add orca -e ... -- orca-mcp` with NO `-s`/`--scope` flag. Claude Code defaults that to LOCAL scope, which binds to one directory. The orchestrator's working directory moved one level up and the MCP server silently vanished from the session — the field is even named `globalInstall`, which is precisely what it does not do. The correct form needs `-s user`. Check the Codex CLI snippet for the same class of defect.

2. THE SAME COMMAND NAMES A BINARY THAT IS NOT ON PATH. `globalInstall` invokes `orca-mcp` (the bin declared in package.json), which only resolves after a global install or link. On this machine `which orca-mcp` returns nothing, so the command as printed fails. The `clients` variant uses absolute paths and works. Two emitted commands, one of which cannot run — decide which is canonical and make the other correct or absent.

3. A PATH-PINNED NODE THAT WILL BREAK ON A TOOLCHAIN BUMP. `bootstrap.nodePath` emits `~/.codex/fnm/node-versions/v24.14.1/installation/bin/node` — a version-pinned path inside another tool's private directory. `~/.codex/fnm/aliases/default/bin/node` survives a Node bump; the pinned path does not. Worth noting the only Node on this machine is the one the Codex CLI bundles, so uninstalling Codex breaks Orca's MCP server entirely. Scope what Orca should do about a toolchain it does not own.

4. THE DAEMON DIES WITH WHOEVER STARTED IT, AND THE FAILURE IS OPAQUE. Started as a session-scoped background process, the daemon was reaped when that session ended. The MCP server still connected fine (it is a stdio client), so from the agent's side everything looked healthy — and the first mutating call failed with the entire message `fetch failed`. That error names no URL, no port, no cause and no remedy. An agent cannot distinguish 'daemon not running' from 'wrong port' from 'expired lease' from 'network'. Scope BOTH halves: a real daemon lifecycle story (start/stop/status, and boot persistence — on macOS that is a LaunchAgent, and Orca currently offers nothing), and an unreachable-daemon error that names the base URL it tried, the likely cause, and the exact command to fix it.

5. THE SAFETY FENCE IS OFF BY DEFAULT AND ONLY WARNS. With `ORCA_REPO_ROOTS` unset the server prints one console line — 'agents may register/work in any folder under your HOME' — and proceeds. It scrolls past instantly and nothing downstream re-raises it, so write-capable executors can be spawned with no fence and nobody is asked. Scope what the right default is, whether an unfenced first run should require an explicit acknowledgement, and how a fence should be discoverable and changeable after the fact. ALSO NOTE A CONSEQUENCE THE FENCE CREATES: with it set to a single workspace root, Orca can no longer orchestrate work on ITS OWN repository, because that repo sits outside the fence. A tool that cannot be used to improve itself is a real limitation — scope multi-root support or an equivalent answer.

6. A CAPABILITY THAT EXISTS BUT IS NOT DISCOVERABLE, AND AN ORCHESTRATOR THAT CANNOT SEE ITS OWN BLIND SPOT. `executor__spawn` accepts a `model` field (registry-lane-create.js:85) and the tool description does list it — but the orchestrator skill doc walks through spawning without ever mentioning that a lane can choose its model. Believing it could not, the orchestrator routed both agents around Orca entirely, through a separate launcher. The result: two agents running on this machine, doing real work in the project directory, with ZERO lane records, no worktree isolation, no audit queue and nothing on the dashboard, while `orchestrator__status` reported a healthy orchestrator. Scope two things: making per-lane model selection obvious at the point of use, and whether `orchestrator__status` should be able to notice and report that an orchestrator has been registered and idle while work is happening in its project — an orchestrator that cannot see work in its own cwd is the failure mode that matters most here.

BEYOND THE SIX, ASSESS AGAINST WHAT AN OUTSIDE ADOPTER WOULD EXPECT: is the first-run path (install, start, register, spawn, audit) documented end-to-end and does it actually work as written; are the MCP tool descriptions sufficient to use each tool correctly WITHOUT reading the source; are errors actionable; is there a health/doctor command that checks daemon reachability, lease validity, fence configuration, MCP registration scope and PATH resolution in one call; and is the lease/token lifecycle (expiry, re-mint, rotation) documented where an agent will actually hit it. Say plainly where Orca is already good — a hardening scope that only lists faults is not an audit.

DELIVER: a ranked defect list, each with file and line, severity, the concrete failure it caused or would cause, and the fix; a proposed first-run and daemon-lifecycle design; the specific documentation and tool-description changes; a DO-NOT-BUILD list of things considered and deliberately rejected, with reasons; and a staged implementation order with cost bands, marking which stages are safe to apply while the daemon is running from this same checkout and which require it stopped. Every stage must be independently landable and testable.

ROLE: SCOPER. You are sandboxed READ-ONLY; do not attempt any write. Deliver findings in your final message ONLY.

SCOPE CONTRACT: Follow this scoped task exactly. Do NOT expand scope, refactor opportunistically, add features, or fix unrelated issues you notice — list those under `notes` instead.

BLOCKED RULE: If you cannot proceed without information or permissions you lack, STOP immediately and return status `blocked` with exactly what you need in `blockedOn`. Do not guess, do not wander, do not try approaches outside the scope.

ACCEPTANCE (what "done" means): A ranked defect list covering all six observed failures plus anything else found, each with file:line, severity, concrete consequence and proposed fix, and each verified against the source rather than taken from the brief; a first-run and daemon-lifecycle design including boot persistence on macOS and an actionable unreachable-daemon error; a doctor/health command proposal; the documentation and MCP tool-description changes needed so each tool is usable without reading source; an explicit statement of what Orca already does well; a defended DO-NOT-BUILD list; and a staged, independently landable implementation order with cost bands, marking which stages are safe to apply while the daemon runs from this checkout. Where the tree contradicts the brief, say so with file and line.

STOP CONDITIONS: done (acceptance met), blocked (need input/permission), or out_of_scope (task can't be done within the fence). Stopping and reporting always beats wandering.

OUTPUT: your FINAL message must be a single JSON object matching the provided output schema (status, summary, filesTouched, evidence, blockedOn, notes). No prose outside the JSON.