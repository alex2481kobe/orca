// Agent tool definitions (the contract data table) + accessors. Extracted from
// agent-tools.js. Intentionally one module: a flat lookup table.

export const TOOL_DEFINITIONS = [
  {
    id: 'session.next_action',
    group: 'session',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'critique', 'dashboard'],
    method: 'GET',
    route: '/api/agent-tools/next-action',
    implemented: true,
    mutating: false,
    summary: 'Fetch the server-approved next legal tool/action envelope.',
  },
  {
    id: 'executor.capabilities',
    group: 'executor',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'critique', 'dashboard'],
    method: 'GET',
    route: '/api/agent-tools/discovery',
    implemented: true,
    mutating: false,
    summary: 'Read the executor capability matrix, including supported roles, modes, intelligence, structured output, and MCP support.',
  },
  {
    id: 'lane.create',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/orchestrators/{orchestratorId}/lanes',
    implemented: true,
    mutating: true,
    summary: 'Create a governed executor lane under your orchestrator container.',
  },
  {
    id: 'lane.list',
    group: 'lane',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/orchestrators/{orchestratorId}/lanes',
    implemented: true,
    mutating: false,
    summary: 'List lanes under an orchestrator (compact: state, owner, executor type, audit state) — your "what is running" view.',
  },
  {
    id: 'lane.get',
    group: 'lane',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}',
    implemented: true,
    mutating: false,
    summary: 'Read one lane in full: logs, agent events, changed files, the captured result (resultText), and processMeta — use this to see WHY a lane failed or what it produced.',
  },
  {
    id: 'lane.terminal.tail',
    group: 'lane',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}/terminal-tail',
    implemented: true,
    mutating: false,
    summary: 'Read a bounded tail of raw terminal.log for near-live worker output. Query: offset, maxBytes. Use nextOffset for incremental polling.',
  },
  {
    id: 'lane.heartbeat',
    group: 'lane',
    roles: ['executor', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/heartbeat',
    implemented: true,
    mutating: true,
    summary: 'Record a worker heartbeat for an active lane.',
  },
  {
    id: 'lane.submit',
    group: 'lane',
    roles: ['executor', 'orchestrator'],
    method: 'POST',
    route: '/api/lanes/{laneId}/submit',
    implemented: true,
    mutating: true,
    summary: 'Submit lane handoff (summary + changed files) and mark ready for audit.',
  },
  {
    id: 'approval.request',
    group: 'approval',
    roles: ['executor', 'critique', 'orchestrator'],
    method: 'POST',
    route: '/api/lanes/{laneId}/approvals',
    implemented: true,
    mutating: true,
    summary: 'Request human/orchestrator approval for a command, patch, or tool action.',
  },
  {
    id: 'approval.list',
    group: 'approval',
    roles: ['supervisor', 'executor', 'orchestrator', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}/approvals',
    implemented: true,
    mutating: false,
    summary: 'List pending and decided approvals for a lane.',
  },
  {
    id: 'approval.respond',
    group: 'approval',
    roles: ['orchestrator', 'dashboard', 'auditor'],
    method: 'POST',
    route: '/api/lanes/{laneId}/approvals/{approvalId}/decide',
    implemented: true,
    mutating: true,
    summary: 'Approve or deny a pending approval on behalf of the user.',
  },
  {
    id: 'lane.shutdown',
    group: 'lane',
    roles: ['orchestrator', 'executor', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/stop',
    implemented: true,
    mutating: true,
    summary: 'Stop or shut down a lane worker.',
  },
  {
    id: 'lane.delete',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'DELETE',
    route: '/api/lanes/{laneId}',
    implemented: true,
    mutating: true,
    summary: 'Permanently delete a terminal lane (done/failed/stopped/accepted/blocked) and its worktree. Refuses a live lane.',
  },
  {
    id: 'lane.retry',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/retry',
    implemented: true,
    mutating: true,
    summary: 'Retry a failed, stopped, or fix-requested lane.',
  },
  {
    id: 'lane.integrate',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/integrate',
    implemented: true,
    mutating: true,
    summary: 'Merge an ISOLATED, audit-accepted lane\'s branch back into the container base branch in the repo root. Reports merged / conflicts / nothing-to-merge. Does not push unless body.push:true. Rejects direct/shared lanes (their work already lives in the checkout).',
  },
  {
    id: 'lane.worktree.discard',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/worktree/discard',
    implemented: true,
    mutating: true,
    summary: 'Discard an isolated lane\'s git worktree. SAFE by default: refuses when the worktree has uncommitted changes. Pass body.force:true to discard them anyway, body.removeBranch:true to also delete the branch.',
  },
  {
    id: 'lane.controls.update',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'PATCH',
    route: '/api/lanes/{laneId}/controls',
    implemented: true,
    mutating: true,
    summary: 'Update a lane\'s controls: model, permissions mode, intelligence, and — when the user left them blank — the targetUrl (dev/preview URL) and verificationCommand the agent has learned for this work.',
  },
  {
    id: 'audit.queue_one',
    group: 'audit',
    roles: ['orchestrator', 'auditor', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit',
    implemented: true,
    mutating: true,
    summary: 'Queue an audit for one lane.',
  },
  {
    id: 'audit.queue_all_ready',
    group: 'audit',
    roles: ['orchestrator', 'auditor', 'dashboard'],
    method: 'POST',
    route: '/api/orchestrators/{orchestratorId}/audit-done-lanes',
    implemented: true,
    mutating: true,
    summary: 'Queue audits for all eligible done lanes under your orchestrator.',
  },
  {
    id: 'audit.findings.record',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/findings',
    implemented: true,
    mutating: true,
    summary: 'Record audit findings and choose accept, request-fix, or block verdict.',
  },
  {
    id: 'audit.accept',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/accept',
    implemented: true,
    mutating: true,
    summary: 'Accept audited lane work.',
  },
  {
    id: 'audit.request_fix',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/request-fix',
    implemented: true,
    mutating: true,
    summary: 'Request a fix pass after audit.',
  },
  {
    id: 'audit.block',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/block',
    implemented: true,
    mutating: true,
    summary: 'Block an audit with a reason.',
  },
  {
    id: 'project.list',
    group: 'projects',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/projects',
    implemented: true,
    mutating: false,
    summary: 'List projects.',
  },
  {
    id: 'project.describe',
    group: 'projects',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/projects/{projectId}',
    implemented: true,
    mutating: false,
    summary: 'Read one project.',
  },
  {
    id: 'project.quick_link.upsert',
    group: 'projects',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/projects/{projectId}/quick-links',
    implemented: true,
    mutating: true,
    summary: 'Create or update a server-authoritative project live link. Include label, url/localUrl/tailnetHttpUrl/httpsServeUrl, kind, port, favorite, and healthPath where known.',
  },
  {
    id: 'project.quick_link.delete',
    group: 'projects',
    roles: ['orchestrator', 'dashboard'],
    method: 'DELETE',
    route: '/api/projects/{projectId}/quick-links/{linkId}',
    implemented: true,
    mutating: true,
    summary: 'Remove one server-authoritative project live link by id.',
  },
  {
    id: 'project.quick_link.health',
    group: 'projects',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/projects/{projectId}/quick-links/{linkId}/check',
    implemented: true,
    mutating: true,
    summary: 'Health-check a validated project live link without accepting arbitrary URLs in the tool call. Body.prefer can be auto|local|tailnet|https; use tailnet for phone/Tailscale checks.',
  },
  {
    id: 'orchestrator.register',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/orchestrators',
    implemented: true,
    mutating: true,
    summary: 'Register as an orchestrator for your working directory. Body: {cwd, actor?, title?, focus?, takeoverOrchestratorId?}. Orca creates the project implicitly (keyed by realpath(cwd)) and binds an orchestrator record to your lease.',
  },
  {
    id: 'orchestrator.update',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'PATCH',
    route: '/api/orchestrators/{orchestratorId}',
    implemented: true,
    mutating: true,
    summary: 'Update your self-authored title/focus line (shown in the dashboard tree). Body: {title?, focus?}.',
  },
  {
    id: 'executor.spawn',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/orchestrators/{orchestratorId}/executors',
    implemented: true,
    mutating: true,
    summary: 'Spawn an executor lane under your orchestrator (runs in your project\'s cwd). Body: {title, executorType, taskPrompt, model?, permissionsProfile?, worktreeMode?, idleShutdownMode?}. worktreeMode: auto (default — read-only/sole-writer run directly in the checkout, overlapping writers get an isolated worktree), direct (force in-place, no worktree), isolated (force a dedicated per-lane worktree), shared (several lanes share the one checkout; conflict risk). Read-only agents never need a worktree. idleShutdownMode: immediate (default — reap the lane if it produces no output/heartbeat for the idle window), short_keepalive (3x the window), policy (never auto-reap; leave to you/the human).',
  },
  {
    id: 'orchestrator.resign',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/orchestrators/{orchestratorId}/resign',
    implemented: true,
    mutating: true,
    summary: 'Release the orchestrator role you hold (mark your orchestrator resigned) so another chat or a human can register/take over.',
  },
  {
    id: 'orchestrator.heartbeat',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/orchestrators/{orchestratorId}/heartbeat',
    implemented: true,
    mutating: true,
    summary: 'Refresh your orchestrator lease (lastSeenAt) so read-only monitoring does not let ownership go stale (~15 min). Call periodically while you hold the orchestrator role.',
  },
  {
    id: 'orchestrator.status',
    group: 'orchestrator',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/orchestrators/{orchestratorId}/status',
    implemented: true,
    mutating: false,
    summary: 'Show who owns this orchestrator plus the lane tree and next required tool — the canonical "what is happening" call.',
  },
  {
    id: 'event.drain',
    group: 'event',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/orchestrators/{orchestratorId}/events/drain',
    implemented: true,
    mutating: false,
    summary: 'Drain unacknowledged durable agent events for your orchestrator, scoped to the caller role/lease. Query: limit, type, afterSeq.',
  },
  {
    id: 'event.replay',
    group: 'event',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/orchestrators/{orchestratorId}/events/replay',
    implemented: true,
    mutating: false,
    summary: 'Replay durable agent event history for your orchestrator without changing ack state. Query: limit, type, afterSeq.',
  },
  {
    id: 'event.ack',
    group: 'event',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/orchestrators/{orchestratorId}/events/ack',
    implemented: true,
    mutating: true,
    summary: 'Acknowledge one or more agent events for the calling consumer. Body: { eventIds: [...] }. Ack state is per role+actor, not global.',
  },
  {
    id: 'tailscale.status',
    group: 'tailscale',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/private-access/tailnet',
    implemented: true,
    mutating: false,
    summary: 'Read Tailscale state: installed, signed in, the device URL, and whether Serve is active (so an agent can tell if Orca is reachable from other devices).',
  },
  {
    id: 'tailscale.serve.configure',
    group: 'tailscale',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/private-access/serve',
    implemented: true,
    mutating: true,
    summary: 'Set up (action:"enable") or tear down (action:"disable") private Tailscale Serve from an admin/workstation surface. Runs the serve command for the user; never enables Funnel.',
  },
  {
    id: 'orca.setup_guide',
    group: 'setup',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/private-access/setup-plan',
    implemented: true,
    mutating: false,
    summary: 'Read the ordered, dry-run setup plan for making Orca reachable from other devices (Tailscale install/sign-in checks, the private Serve command, and pairing). Use this to set Orca up for a user step by step.',
  },
];

function publicTool(tool) {
  return {
    id: tool.id,
    group: tool.group,
    roles: [...tool.roles],
    method: tool.method,
    route: tool.route,
    implemented: Boolean(tool.implemented),
    mutating: Boolean(tool.mutating),
    summary: tool.summary,
  };
}

export function getToolDefinitions() {
  return TOOL_DEFINITIONS.map(publicTool);
}

export function findTool(toolId) {
  return TOOL_DEFINITIONS.find((tool) => tool.id === toolId) || null;
}
