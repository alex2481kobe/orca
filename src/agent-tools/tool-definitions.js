// Agent tool definitions (the contract data table) + accessors. Extracted from
// agent-tools.js. Intentionally one module: a flat lookup table.

export const TOOL_DEFINITIONS = [
  {
    id: 'session.describe',
    group: 'session',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'critique', 'dashboard'],
    method: 'GET',
    route: '/api/sessions/{sessionId}',
    implemented: true,
    mutating: false,
    summary: 'Read session state and lane summary.',
  },
  {
    id: 'session.plan.update',
    group: 'session',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/plan',
    implemented: true,
    mutating: true,
    summary: 'Update the durable session goal and task plan (free-text). For structured, fan-out-able work use the task.* backlog tools instead.',
  },
  {
    id: 'session.create',
    group: 'session',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/projects/{projectId}/sessions',
    implemented: true,
    mutating: true,
    summary: 'Create a session in a project. Body: name (required), leader (executor type), approvedCapacity, spawnPolicy (never|ask|within_capacity|auto), worktreeMode (isolated|shared), repoRoot, defaultModel. Use spawnPolicy:"auto" to let the backlog fan out automatically.',
  },
  {
    id: 'session.list',
    group: 'session',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/projects/{projectId}/sessions',
    implemented: true,
    mutating: false,
    summary: 'List active sessions for a project.',
  },
  {
    id: 'session.next_action',
    group: 'session',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'critique', 'dashboard'],
    method: 'GET',
    route: '/api/agent-tools/next-action',
    implemented: true,
    mutating: false,
    summary: 'Fetch the server-approved next legal tool/action envelope.',
  },
  {
    id: 'executor.capabilities',
    group: 'executor',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'critique', 'dashboard'],
    method: 'GET',
    route: '/api/agent-tools/discovery',
    implemented: true,
    mutating: false,
    summary: 'Read the executor capability matrix, including supported roles, modes, intelligence, structured output, and MCP support.',
  },
  {
    id: 'supervisor.overview',
    group: 'supervisor',
    roles: ['supervisor', 'dashboard'],
    method: 'GET',
    route: '/api/supervisor/overview',
    implemented: true,
    mutating: false,
    summary: 'Read the cross-project supervisor view: projects, sessions, active orchestrators, attention queue, backlog roll-ups, warnings, and next required tools.',
  },
  {
    id: 'supervisor.resign',
    group: 'supervisor',
    roles: ['supervisor'],
    method: 'POST',
    route: '/api/supervisor/resign',
    implemented: true,
    mutating: true,
    summary: 'Detach this supervisor MCP client by revoking only its own supervisor tool lease.',
  },
  {
    id: 'lane.create',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/lanes',
    implemented: true,
    mutating: true,
    summary: 'Create a governed lane in a session.',
  },
  {
    id: 'lane.list',
    group: 'lane',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/sessions/{sessionId}/lanes',
    implemented: true,
    mutating: false,
    summary: 'List lanes in a session (compact: state, owner, executor type, audit/critique state) — your "what is running" view.',
  },
  {
    id: 'lane.get',
    group: 'lane',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}',
    implemented: true,
    mutating: false,
    summary: 'Read one lane in full: logs, agent events, changed files, the captured result (resultText), and processMeta — use this to see WHY a lane failed or what it produced.',
  },
  {
    id: 'lane.terminal.tail',
    group: 'lane',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}/terminal-tail',
    implemented: true,
    mutating: false,
    summary: 'Read a bounded tail of raw terminal.log for near-live worker output. Query: offset, maxBytes. Use nextOffset for incremental polling.',
  },
  {
    id: 'lane.heartbeat',
    group: 'lane',
    roles: ['executor', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/heartbeat',
    implemented: true,
    mutating: true,
    summary: 'Record a worker heartbeat for an active lane.',
  },
  {
    id: 'lane.submit',
    group: 'lane',
    roles: ['executor', 'orchestrator'],
    method: 'POST',
    route: '/api/lanes/{laneId}/submit',
    implemented: true,
    mutating: true,
    summary: 'Submit lane handoff (summary + changed files) and mark ready for audit.',
  },
  {
    id: 'approval.request',
    group: 'approval',
    roles: ['executor', 'critique', 'orchestrator'],
    method: 'POST',
    route: '/api/lanes/{laneId}/approvals',
    implemented: true,
    mutating: true,
    summary: 'Request human/orchestrator approval for a command, patch, or tool action.',
  },
  {
    id: 'approval.list',
    group: 'approval',
    roles: ['supervisor', 'executor', 'orchestrator', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}/approvals',
    implemented: true,
    mutating: false,
    summary: 'List pending and decided approvals for a lane.',
  },
  {
    id: 'approval.respond',
    group: 'approval',
    roles: ['orchestrator', 'dashboard', 'auditor'],
    method: 'POST',
    route: '/api/lanes/{laneId}/approvals/{approvalId}/decide',
    implemented: true,
    mutating: true,
    summary: 'Approve or deny a pending approval on behalf of the user.',
  },
  {
    id: 'lane.shutdown',
    group: 'lane',
    roles: ['orchestrator', 'executor', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/stop',
    implemented: true,
    mutating: true,
    summary: 'Stop or shut down a lane worker.',
  },
  {
    id: 'lane.delete',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'DELETE',
    route: '/api/lanes/{laneId}',
    implemented: true,
    mutating: true,
    summary: 'Permanently delete a terminal lane (done/failed/stopped/accepted/blocked) and its worktree. Refuses a live lane.',
  },
  {
    id: 'lane.retry',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/retry',
    implemented: true,
    mutating: true,
    summary: 'Retry a failed, stopped, or fix-requested lane.',
  },
  {
    id: 'lane.controls.update',
    group: 'lane',
    roles: ['orchestrator', 'dashboard'],
    method: 'PATCH',
    route: '/api/lanes/{laneId}/controls',
    implemented: true,
    mutating: true,
    summary: 'Update a lane\'s controls: model, permissions mode, intelligence, and — when the user left them blank — the targetUrl (dev/preview URL) and verificationCommand the agent has learned for this work.',
  },
  {
    id: 'capacity.request',
    group: 'capacity',
    roles: ['orchestrator'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/capacity/request',
    implemented: true,
    mutating: true,
    summary: 'Request additional executor capacity.',
  },
  {
    id: 'capacity.approve',
    group: 'capacity',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/capacity/requests/{requestId}/approve',
    implemented: true,
    mutating: true,
    summary: 'Approve a structured capacity request.',
  },
  {
    id: 'capacity.reject',
    group: 'capacity',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/capacity/requests/{requestId}/reject',
    implemented: true,
    mutating: true,
    summary: 'Reject a structured capacity request.',
  },
  {
    id: 'capacity.set_policy',
    group: 'capacity',
    roles: ['dashboard', 'orchestrator'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/capacity/policy',
    implemented: true,
    mutating: true,
    summary: 'Update capacity, spawn policy, idle shutdown, solo mode, and optional worktreeMode (isolated|shared).',
  },
  {
    id: 'session.worktree_policy.update',
    group: 'session',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/worktree-policy',
    implemented: true,
    mutating: true,
    summary: 'Update the session worktree mode. Body: worktreeMode "isolated" (per-lane git worktrees when possible) or "shared" (all lanes use the session repoRoot/shared folder; conflict-risk warnings apply).',
  },
  {
    id: 'critique.bundle.create',
    group: 'critique',
    roles: ['executor', 'orchestrator', 'critique', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/critique/bundle',
    implemented: true,
    mutating: true,
    summary: 'Create a self-verification bundle for the current lane revision.',
  },
  {
    id: 'critique.findings.record',
    group: 'critique',
    roles: ['executor', 'orchestrator', 'critique', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/critique/findings',
    implemented: true,
    mutating: true,
    summary: 'Record critique findings against a nonce/revision.',
  },
  {
    id: 'critique.waive',
    group: 'critique',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/critique/waive',
    implemented: true,
    mutating: true,
    summary: 'Waive critique with reason and audit record.',
  },
  {
    id: 'audit.queue_one',
    group: 'audit',
    roles: ['orchestrator', 'auditor', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit',
    implemented: true,
    mutating: true,
    summary: 'Queue an audit for one lane.',
  },
  {
    id: 'audit.queue_all_ready',
    group: 'audit',
    roles: ['orchestrator', 'auditor', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/audit-done-lanes',
    implemented: true,
    mutating: true,
    summary: 'Queue audits for all eligible done lanes in a session.',
  },
  {
    id: 'audit.findings.record',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/findings',
    implemented: true,
    mutating: true,
    summary: 'Record audit findings and choose accept, request-fix, or block verdict.',
  },
  {
    id: 'audit.accept',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/accept',
    implemented: true,
    mutating: true,
    summary: 'Accept audited lane work.',
  },
  {
    id: 'audit.request_fix',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/request-fix',
    implemented: true,
    mutating: true,
    summary: 'Request a fix pass after audit.',
  },
  {
    id: 'audit.block',
    group: 'audit',
    roles: ['auditor', 'orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/audit/block',
    implemented: true,
    mutating: true,
    summary: 'Block an audit with a reason.',
  },
  {
    id: 'evidence.capture_screenshot',
    group: 'evidence',
    roles: ['executor', 'orchestrator', 'critique', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/evidence',
    implemented: true,
    mutating: true,
    summary: 'Capture screenshot evidence for a lane target.',
  },
  {
    id: 'evidence.capture_video',
    group: 'evidence',
    roles: ['executor', 'orchestrator', 'critique', 'dashboard'],
    method: 'POST',
    route: '/api/lanes/{laneId}/evidence',
    implemented: true,
    mutating: true,
    summary: 'Capture optional video evidence for user inspection.',
  },
  {
    id: 'evidence.list',
    group: 'evidence',
    roles: ['supervisor', 'executor', 'orchestrator', 'auditor', 'critique', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}/evidence',
    implemented: true,
    mutating: false,
    summary: 'List evidence artifacts for a lane.',
  },
  {
    id: 'evidence.latest',
    group: 'evidence',
    roles: ['supervisor', 'executor', 'orchestrator', 'auditor', 'critique', 'dashboard'],
    method: 'GET',
    route: '/api/lanes/{laneId}/evidence/latest',
    implemented: true,
    mutating: false,
    summary: 'Fetch latest evidence metadata for a lane.',
  },
  {
    id: 'evidence.cleanup_dry_run',
    group: 'evidence',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/artifacts/cleanup',
    implemented: true,
    mutating: true,
    summary: 'Dry-run artifact/evidence cleanup.',
  },
  {
    id: 'evidence.cleanup_apply',
    group: 'evidence',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/artifacts/cleanup',
    implemented: true,
    mutating: true,
    summary: 'Apply approved artifact/evidence cleanup.',
  },
  {
    id: 'provider.list',
    group: 'providers',
    roles: ['orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/providers',
    implemented: true,
    mutating: false,
    summary: 'List provider profiles and non-secret credential status.',
  },
  {
    id: 'provider.health',
    group: 'providers',
    roles: ['orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/providers/{providerId}/health',
    implemented: true,
    mutating: false,
    summary: 'Check provider health without exposing secrets.',
  },
  {
    id: 'provider.configure',
    group: 'providers',
    roles: ['dashboard'],
    method: 'PATCH',
    route: '/api/providers/{providerId}',
    implemented: true,
    mutating: true,
    summary: 'Update a provider profile with approval.',
  },
  {
    id: 'provider.secret.set',
    group: 'providers',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/providers/{providerId}/secret',
    implemented: true,
    mutating: true,
    summary: 'Store a provider secret in the configured credential backend.',
  },
  {
    id: 'provider.secret.delete',
    group: 'providers',
    roles: ['dashboard'],
    method: 'DELETE',
    route: '/api/providers/{providerId}/secret',
    implemented: true,
    mutating: true,
    summary: 'Delete a stored provider secret reference.',
  },
  {
    id: 'project.list',
    group: 'projects',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/projects',
    implemented: true,
    mutating: false,
    summary: 'List projects.',
  },
  {
    id: 'project.create',
    group: 'projects',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/projects',
    implemented: true,
    mutating: true,
    summary: 'Create a project container for an agent-first cold start with an unscoped lease. Body: name, slug, repoRoot, approved.',
  },
  {
    id: 'project.describe',
    group: 'projects',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/projects/{projectId}',
    implemented: true,
    mutating: false,
    summary: 'Read one project.',
  },
  {
    id: 'project.quick_link.upsert',
    group: 'projects',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/projects/{projectId}/quick-links',
    implemented: true,
    mutating: true,
    summary: 'Create or update a server-authoritative project live link. Include label, url/localUrl/tailnetHttpUrl/httpsServeUrl, kind, port, favorite, and healthPath where known.',
  },
  {
    id: 'project.quick_link.delete',
    group: 'projects',
    roles: ['orchestrator', 'dashboard'],
    method: 'DELETE',
    route: '/api/projects/{projectId}/quick-links/{linkId}',
    implemented: true,
    mutating: true,
    summary: 'Remove one server-authoritative project live link by id.',
  },
  {
    id: 'project.quick_link.health',
    group: 'projects',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/projects/{projectId}/quick-links/{linkId}/check',
    implemented: true,
    mutating: true,
    summary: 'Health-check a validated project live link without accepting arbitrary URLs in the tool call. Body.prefer can be auto|local|tailnet|https; use tailnet for phone/Tailscale checks.',
  },
  {
    id: 'project.archive',
    group: 'projects',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/projects/{projectId}/archive',
    implemented: true,
    mutating: true,
    summary: 'Archive a project without deleting state.',
  },
  {
    id: 'project.restore',
    group: 'projects',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/projects/{projectId}/restore',
    implemented: true,
    mutating: true,
    summary: 'Restore an archived project.',
  },
  {
    id: 'settings.describe_effective',
    group: 'settings',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/policy',
    implemented: true,
    mutating: false,
    summary: 'Read effective policy/settings snapshot.',
  },
  {
    id: 'settings.update',
    group: 'settings',
    roles: ['orchestrator', 'dashboard'],
    method: 'PATCH',
    route: '/api/settings/{scope}/{id}',
    implemented: true,
    mutating: true,
    summary: 'Update scoped settings overrides. scope is project|session|lane, id is that record\'s id; body.settingsOverrides carries the layered overrides (e.g. flow.auditTier "separate-auditor", flow.requireAuditPass, spawn.approvedCapacity).',
  },
  {
    id: 'settings.export',
    group: 'settings',
    roles: ['dashboard'],
    method: 'GET',
    route: '/api/providers/export',
    implemented: true,
    mutating: false,
    summary: 'Export non-secret provider/settings config.',
  },
  {
    id: 'settings.import_dry_run',
    group: 'settings',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/providers/import/dry-run',
    implemented: true,
    mutating: true,
    summary: 'Validate settings/profile import without applying it.',
  },
  {
    id: 'settings.import_apply',
    group: 'settings',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/providers/import/apply',
    implemented: true,
    mutating: true,
    summary: 'Apply validated settings/profile import with approval.',
  },
  {
    id: 'orchestrator.enroll',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/orchestrator/enroll',
    implemented: true,
    mutating: true,
    summary: 'Become this session\'s active orchestrator (bind your lease + claim ownership). Body.takeover=true to take over from an existing active orchestrator.',
  },
  {
    id: 'orchestrator.resign',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/orchestrator/resign',
    implemented: true,
    mutating: true,
    summary: 'Release the active-orchestrator role you hold for this session so another chat or a human can take over.',
  },
  {
    id: 'orchestrator.thread.get',
    group: 'orchestrator',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/sessions/{sessionId}/orchestrator',
    implemented: true,
    mutating: false,
    summary: 'Read the existing server-owned orchestrator thread/messages for a session without taking ownership or spawning a new orchestrator.',
  },
  {
    id: 'orchestrator.status',
    group: 'orchestrator',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/sessions/{sessionId}/orchestrator/status',
    implemented: true,
    mutating: false,
    summary: 'Show who owns this session as orchestrator plus the lane tree, backlog roll-up, and next required tool — the canonical "what is happening" call.',
  },
  {
    id: 'orchestrator.message.send',
    group: 'orchestrator',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/orchestrator/messages',
    implemented: true,
    mutating: true,
    summary: 'Queue a server-owned orchestrator turn/message for this session. Lease callers must be the active orchestrator.',
  },
  {
    id: 'session.supervisor_audit',
    group: 'supervisor',
    roles: ['supervisor', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/supervisor/audit',
    implemented: true,
    mutating: true,
    summary: 'Record supervisor review of a session/orchestrator outcome. Body: verdict accept|request_fix|block, summary, findings, nextTask, plan.',
  },
  {
    id: 'task.add',
    group: 'task',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/tasks',
    implemented: true,
    mutating: true,
    summary: 'Add one durable task to the session backlog. Body: title (required), description, taskPrompt, executorType, model, priority, maxAttempts, targetUrl, verificationCommand.',
  },
  {
    id: 'task.bulk_add',
    group: 'task',
    roles: ['orchestrator', 'dashboard'],
    method: 'POST',
    route: '/api/sessions/{sessionId}/tasks/bulk',
    implemented: true,
    mutating: true,
    summary: 'Add many backlog tasks at once. Body: { tasks: [{ title, description, ... }] }. Use this to load a whole to-do list the orchestrator will fan out.',
  },
  {
    id: 'task.list',
    group: 'task',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/sessions/{sessionId}/tasks',
    implemented: true,
    mutating: false,
    summary: 'List backlog tasks with state (pending/assigned/in_lane/accepted/failed/blocked), priority, and the linked lane id.',
  },
  {
    id: 'task.update',
    group: 'task',
    roles: ['orchestrator', 'dashboard'],
    method: 'PATCH',
    route: '/api/tasks/{taskId}',
    implemented: true,
    mutating: true,
    summary: 'Update a backlog task (priority/fields) or change its state — set state:"blocked" (with reason) or state:"pending" to unblock/requeue.',
  },
  {
    id: 'task.delete',
    group: 'task',
    roles: ['orchestrator', 'dashboard'],
    method: 'DELETE',
    route: '/api/tasks/{taskId}',
    implemented: true,
    mutating: true,
    summary: 'Delete a backlog task that is not currently running in a lane.',
  },
  {
    id: 'backlog.status',
    group: 'backlog',
    roles: ['supervisor', 'orchestrator', 'executor', 'auditor', 'dashboard'],
    method: 'GET',
    route: '/api/sessions/{sessionId}/backlog',
    implemented: true,
    mutating: false,
    summary: 'Read the backlog roll-up: counts per state, capacity/spawn policy, and the batch-completion signal (complete / allAccepted).',
  },
  {
    id: 'tailscale.status',
    group: 'tailscale',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/private-access/tailnet',
    implemented: true,
    mutating: false,
    summary: 'Read Tailscale state: installed, signed in, the device URL, and whether Serve is active (so an agent can tell if Orca is reachable from other devices).',
  },
  {
    id: 'tailscale.serve.configure',
    group: 'tailscale',
    roles: ['dashboard'],
    method: 'POST',
    route: '/api/private-access/serve',
    implemented: true,
    mutating: true,
    summary: 'Set up (action:"enable") or tear down (action:"disable") private Tailscale Serve from an admin/workstation surface. Runs the serve command for the user; never enables Funnel.',
  },
  {
    id: 'orca.setup_guide',
    group: 'setup',
    roles: ['supervisor', 'orchestrator', 'dashboard'],
    method: 'GET',
    route: '/api/private-access/setup-plan',
    implemented: true,
    mutating: false,
    summary: 'Read the ordered, dry-run setup plan for making Orca reachable from other devices (Tailscale install/sign-in checks, the private Serve command, and pairing). Use this to set Orca up for a user step by step.',
  },
];

export function publicTool(tool) {
  return {
    id: tool.id,
    group: tool.group,
    roles: [...tool.roles],
    method: tool.method,
    route: tool.route,
    implemented: Boolean(tool.implemented),
    mutating: Boolean(tool.mutating),
    summary: tool.summary,
  };
}

export function getToolDefinitions() {
  return TOOL_DEFINITIONS.map(publicTool);
}

export function findTool(toolId) {
  return TOOL_DEFINITIONS.find((tool) => tool.id === toolId) || null;
}
