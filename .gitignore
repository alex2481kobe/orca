artifacts/
.orca/
node_modules/
*.log
*.log.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
.env
.env.*
.tauri/
coverage/
dist/
build/
.tmp/
tmp/
*.pid
*.seed
*.swp
*.swo
*.swn
.verify-out.txt
tmp-visual/

# Local-only docs (never published). The `.local.md` suffix marks working notes
# that must stay off the public repo; also the private roadmap/task docs that
# CLAUDE.md references but are intentionally untracked, plus the maintainer
# release/GitHub-settings runbook (owner ops — kept local per the contribution
# policy's "keep owner setting checklists local" rule).
*.local.md
docs/roadmap.md
docs/current-task.md
docs/full-buildout-goal.md
docs/open-source-release-checklist.md
docs/final-readiness-report.md

# Local-only workspace coordination. These lived in the parent wrapper folder
# before the repo was flattened to a single `orca/` root; they are local
# governance/scratch and must never be published.
throwaway/
.claudeignore
.codexignore
.claude/

# Native wrapper is parked on the experimental branch; kept out of main.
src-tauri/
